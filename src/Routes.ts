export enum Routes {
  // Dashboard
  dashboard = "/dashboard",
  // Teams
  teams = "/teams",
  teamFavorites = "/teams/favorites",
  teamOrg = "/teams/org",
  teamMembers = "/teams/members",
  teamsAssets = "/teams/assets",
}
